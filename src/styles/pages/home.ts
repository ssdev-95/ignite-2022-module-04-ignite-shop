import { styled } from '../'

export const HomeContainer = styled('main', {
  width: '100%',
  maxWidth: 'calc(100vw - ((100vw - 1180px) / 2))',
  minHeight: '656px',
  display: 'flex',
  marginLeft: 'auto',
  overflowY: 'hidden',
})
